// services/authService.ts
// - 인증 관련 서비스: 실제 백엔드 로그인/로그아웃 API로 대체하세요.
export const authService = {
  login: async (id: string, pw: string) => {
    // placeholder
    return { ok: true, token: 'fake' };
  }
};
